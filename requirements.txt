fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# langchain 계열은 로컬 검증 버전(0.3.x / vertexai 2.1.x)으로 메이저 핀.
# vertexai 3.x 는 numexpr 의존성이 생겨 arm64 Lambda 이미지에서 소스
# 컴파일을 유발 (2026-06-07 배포 실패, run 27069476418).
langchain>=0.3.0,<1
langchain-google-vertexai>=2.0.0,<3
langchain-core>=0.3.0,<1
# Lambda python:3.11 베이스 = Amazon Linux 2 (glibc 2.26). 최신
# numpy/numexpr 휠은 manylinux_2_28(glibc 2.28+) 전용이라 AL2 에서
# 소스 컴파일로 떨어져 빌드 실패 (run 27069589258). manylinux2014
# 휠이 있는 버전으로 핀. numexpr<2.10 은 numpy 1.x ABI 빌드라
# numpy<2 와 반드시 세트로 유지 (섞으면 런타임 크래시).
numpy>=1.26,<2
numexpr>=2.8.6,<2.10
pydantic>=2.0.0
python-dotenv>=1.0.0
mangum>=0.17.0
