services:
  feeling-palette-api:
    build: .
    container_name: feeling-palette-api
    restart: unless-stopped
    ports:
      - "8100:8080"
    environment:
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
