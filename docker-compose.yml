services:
  feeling-palette-api:
    build: .
    container_name: feeling-palette-api
    restart: unless-stopped
    ports:
      - "8100:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
